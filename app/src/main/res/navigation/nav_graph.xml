<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mapFragment">

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.example.locmark.MapFragment"
        android:label="Map" />
    <fragment
        android:id="@+id/entityFormFragment"
        android:name="com.example.locmark.EntityFormFragment"
        android:label="Form">
        <argument
            android:name="entityId"
            app:argType="integer"
            android:defaultValue="-1" />
    </fragment>
    <fragment
        android:id="@+id/entityListFragment"
        android:name="com.example.locmark.EntityListFragment"
        android:label="List" >
        <action
            android:id="@+id/action_entityListFragment_to_entityFormFragment"
            app:destination="@id/entityFormFragment" />
    </fragment>
</navigation>
